import 'package:flutter/material.dart';

class TextButtonA extends StatelessWidget {
  const TextButtonA({super.key});

  @override
  Widget build(BuildContext context) {
    return TextButton(
      onPressed: () {},
      child: Text(''),
    );
  }
}
